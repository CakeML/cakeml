INCLUDES = .. ../standalone ../repl ../repl/proofs ../compiler/proofs ../translator ../translator/repl ../bytecode
OPTIONS = QUIT_ON_FAILURE

ifdef POLY
HOLHEAP = heap
EXTRA_CLEANS = $(HOLHEAP) $(HOLHEAP).o

BARE_THYS = ../repl/proofs/replCorrectTheory ../translator/repl/ml_repl_moduleTheory

DEPS = $(patsubst %,%.uo,$(BARE_THYS))

$(HOLHEAP): $(DEPS)
	$(protect $(HOLDIR)/bin/buildheap) -b ../standalone/heap -o $(HOLHEAP) $(BARE_THYS)
endif
