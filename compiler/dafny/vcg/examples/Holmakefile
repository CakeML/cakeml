INCLUDES = $(CAKEMLDIR)/compiler/dafny/vcg $(CAKEMLDIR)/compiler/dafny/proofs $(CAKEMLDIR)/translator

all: $(DEFAULT_TARGETS) README.md
.PHONY: all

sinclude $(CAKEMLDIR)/developers/readme.mk

ifdef POLY
HOLHEAP = $(CAKEMLDIR)/misc/cakeml-heap
endif
