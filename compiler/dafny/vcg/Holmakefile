INCLUDES = $(CAKEMLDIR)/compiler/dafny $(CAKEMLDIR)/compiler/dafny/semantics $(HOLDIR)/examples/algorithms

all: $(DEFAULT_TARGETS) README.md
.PHONY: all

sinclude $(CAKEMLDIR)/developers/readme.mk

ifdef POLY
HOLHEAP = $(CAKEMLDIR)/misc/cakeml-heap
endif
