INCLUDES = $(CAKEMLDIR)/misc $(CAKEMLDIR)/basis/pure $(CAKEMLDIR)/compiler/dafny

all: $(DEFAULT_TARGETS) README.md
.PHONY: all

sinclude $(CAKEMLDIR)/developers/readme.mk

ifdef POLY
HOLHEAP = $(CAKEMLDIR)/misc/cakeml-heap
endif
