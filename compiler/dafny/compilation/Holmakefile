INCLUDES = $(CAKEMLDIR)/misc $(CAKEMLDIR)/compiler/dafny/translation $(CAKEMLDIR)/cv_translator

all: $(DEFAULT_TARGETS) README.md dafny_compiler
.PHONY: all

sinclude $(CAKEMLDIR)/developers/readme.mk

ifdef POLY
HOLHEAP = $(CAKEMLDIR)/cv_translator/cake_compile_heap
endif

dafny_compiler: dafny_compilerCompileTheory.uo
	gcc -o dafny_compiler dafny_compiler.S $(CAKEMLDIR)/basis/basis_ffi.c -lm
