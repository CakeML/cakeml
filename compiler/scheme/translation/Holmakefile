INCLUDES = $(CAKEMLDIR)/compiler/scheme $(CAKEMLDIR)/translator # $(CAKEMLDIR)/basis

all: $(DEFAULT_TARGETS) README.md
.PHONY: all

sinclude $(CAKEMLDIR)/developers/readme.mk

ifdef POLY
HOLHEAP = $(CAKEMLDIR)/basis/basis-heap
endif
