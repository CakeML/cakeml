bytecode.lem
  definition of bytecode language

bytecodeEvalScript.sml
  functional implementing the deterministic bytecode evaluation relation

bytecodeExtraScript.sml
  various theorems and helpful definitions about the bytecode

bytecodeEncodeScript.sml
  theory about encoding bytecode as numbers or strings (used for the unverified
  interpreter)

bytecodeClockScript.sml
  proofs about clocked bytecode evaluation, used for the divergence
  preservation aspect of compiler correctness

bytecodeLabelsScript.sml
  definition and proofs about routines for replacing bytecode labels by
  concrete addresses

labels_computeLib.sml
  library for removing labels by evaluation in the logic

bytecodeTerminationScript.sml
  termination proofs for recursive functions
