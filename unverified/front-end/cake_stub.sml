(* SML source file required by code produced by CakeML -> SML translation *)
structure Cake_stub = struct
  type 'a ref = 'a ref
  type exn = exn
  type unit = unit
  val op := = op :=
  val ! = !
  fun my_eq x y = x = y
end
