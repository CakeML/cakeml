Unverified tools.

interp/ 
  a CakeML interpreter written in Haskell.  It tries to give reasonable parse
  and type error messages that include source locations.

bytecode/
  an interpreter for CakeML bytecode written in C.  For performance testing.
