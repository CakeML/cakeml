(*Generated by Lem from ./ffi/simpleIO.lem.*)
open HolKernel Parse boolLib bossLib;
open lem_pervasivesTheory lem_pervasives_extraTheory libTheory ffiTheory;

val _ = numLib.prefer_num();



val _ = new_theory "simpleIO"

(*
  A simple instantiation of the ffi type.
*)
(*open import Pervasives*)
(*open import Pervasives_extra*)
(*open import Lib*)
(*open import Ffi*)

val _ = Hol_datatype `
 simpleIO = <| input :  word8 llist; output :  word8 llist |>`;


(*val isEof : oracle_function simpleIO*)
val _ = Define `
 ((isEof:simpleIO ->(word8)list ->(word8)list ->(simpleIO)oracle_result) st conf input1=
   ((case input1 of
    [] => Oracle_final FFI_failed
  | x::xs => Oracle_return st ((if st.input = LNIL then n2w (( 1 : num)) else n2w (( 0 : num)))::xs)
  )))`;


(*val getChar : oracle_function simpleIO*)
val _ = Define `
 ((getChar:simpleIO ->(word8)list ->(word8)list ->(simpleIO)oracle_result) st conf input1=
   ((case input1 of
    [] => Oracle_final FFI_failed
  | x::xs =>
      (case LHD st.input of
        SOME y => Oracle_return (( st with<| input := (THE (LTL st.input)) |>)) (y::xs)
      | _ => Oracle_final FFI_failed
      )
  )))`;


(*val putChar : oracle_function simpleIO*)
val _ = Define `
 ((putChar:simpleIO ->(word8)list ->(word8)list ->(simpleIO)oracle_result) st conf input1=
   ((case input1 of
    [] => Oracle_final FFI_failed
  | x::_ => Oracle_return (( st with<| output := (LCONS x st.output) |>)) input1
  )))`;


(*val exit : oracle_function simpleIO*)
val _ = Define `
 ((exit:simpleIO ->(word8)list ->(word8)list ->(simpleIO)oracle_result) st conf input1=  (Oracle_final FFI_diverged))`;


(*val simpleIO_oracle : oracle simpleIO*)
val _ = Define `
 ((simpleIO_oracle:string -> simpleIO ->(word8)list ->(word8)list ->(simpleIO)oracle_result) s st conf input1=
   (if s = "isEof" then
    isEof st conf input1
  else if s = "getChar" then
    getChar st conf input1
  else if s = "putChar" then
    putChar st conf input1
  else if s = "exit" then
    exit st conf input1
  else
    Oracle_final FFI_failed))`;

val _ = export_theory()

